# Wrangler writes these during `wrangler deploy`; they are build scratch, not source.
.wrangler/
node_modules/
tests/node_modules/
.firebase/

# OS / editor noise
.DS_Store
Thumbs.db

# Emulator scratch output.
tests/firestore-debug.log
tests/ui-debug.log
